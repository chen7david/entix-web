import { HashRouter } from "react-router-dom";
import { RouterContent } from "./routes";

function App() {
  return (
    <HashRouter>
      <RouterContent />
    </HashRouter>
  );
}

export default App;
